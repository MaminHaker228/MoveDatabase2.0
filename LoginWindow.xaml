<Window x:Class="MovieDatabase.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему - Movie Database" 
        Height="650" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <StackPanel>
                <TextBlock Text="🎬 Movie Database" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Система управления фильмами" 
                          FontSize="14" 
                          Foreground="White"
                          Opacity="0.9"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Форма входа -->
        <Border Grid.Row="1" Background="White" 
                Margin="30,30,30,30" 
                Padding="30"
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" 
                                ShadowDepth="2" 
                                Opacity="0.2" 
                                Color="#666"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Войдите в систему" 
                          FontSize="20" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,25"
                          HorizontalAlignment="Center"/>

                <!-- Логин -->
                <TextBlock Text="Логин:" FontSize="14" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameTextBox" 
                        Style="{StaticResource ModernTextBox}"
                        Margin="0,0,0,15"
                        />

                <!-- Пароль -->
                <TextBlock Text="Пароль:" FontSize="14" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" 
                            Style="{StaticResource ModernPasswordBox}"
                            Margin="0,0,0,5"
                            />

                <!-- Сообщение об ошибке -->
                <TextBlock x:Name="ErrorMessageText" 
                          Text="" 
                          Foreground="Red"
                          FontSize="12"
                          Margin="0,0,0,15"
                          TextWrapping="Wrap"
                          Visibility="Collapsed"/>

                <!-- Кнопки -->
                <Button Content="Войти" 
                       Style="{StaticResource PrimaryButton}"
                       Click="LoginButton_Click"
                       Margin="0,10,0,10"
                       Height="40"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>