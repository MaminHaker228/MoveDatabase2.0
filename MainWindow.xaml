<Window x:Class="MovieDatabase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Movie Database - Каталог фильмов" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🎬" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Movie Database" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="White"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="UserNameText" 
                              Text="Пользователь" 
                              FontSize="14" 
                              Foreground="White"
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"/>
                    <Button Content="Выход" 
                           Style="{StaticResource SecondaryButton}"
                           Click="LogoutButton_Click"
                           Height="35"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Панель поиска и фильтров -->
        <Border Grid.Row="1" Background="White" Padding="15" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Поиск -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Поиск:" 
                              VerticalAlignment="Center" 
                              Margin="0,0,10,0"
                              FontSize="14"/>
                    <TextBox x:Name="SearchTextBox" 
                            Style="{StaticResource ModernTextBox}"
                            Width="300"
                            TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>

                <!-- Категория -->
                <StackPanel Orientation="Horizontal" Margin="749,0,0,0" Grid.ColumnSpan="2">
                    <TextBlock Text="Категория:" 
                              VerticalAlignment="Center" 
                              Margin="0,0,10,0"
                              FontSize="14"/>
                    <ComboBox x:Name="CategoryComboBox" 
                             Style="{StaticResource ModernComboBox}"
                             Width="150"
                             SelectionChanged="CategoryComboBox_SelectionChanged"/>
                </StackPanel>

                <!-- Кнопка сброса -->
                <Button Grid.Column="2" 
                       Content="Сбросить фильтры" 
                       Style="{StaticResource SecondaryButton}"
                       Click="ResetFiltersButton_Click"
                       Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <!-- Список фильмов -->
        <DataGrid Grid.Row="2" 
                 x:Name="MoviesDataGrid"
                 AutoGenerateColumns="False"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 CanUserResizeRows="False"
                 RowHeaderWidth="0"
                 GridLinesVisibility="Horizontal"
                 HorizontalGridLinesBrush="#EEE"
                 Background="White"
                 BorderThickness="0"
                 Margin="15"
                 MouseDoubleClick="MoviesDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding MovieId}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*" MinWidth="200"/>
                <DataGridTextColumn Header="Год" Binding="{Binding Year}" Width="80"/>
                <DataGridTextColumn Header="Режиссёр" Binding="{Binding Director}" Width="150"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="120"/>
                <DataGridTextColumn Header="Рейтинг" Binding="{Binding Rating}" Width="80"/>
                <DataGridTextColumn Header="Длительность (мин)" Binding="{Binding Duration}" Width="130"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F5F5F5"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Статус-бар -->
        <Border Grid.Row="3" Background="#F5F5F5" Padding="15,10" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" 
                      Text="Загрузка данных..." 
                      FontSize="12" 
                      Foreground="#666"/>
        </Border>
    </Grid>
</Window>